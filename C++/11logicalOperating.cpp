#include <iostream>

int main() {
    int num = 1;
    if (num == 0 || num == 1) { // use || instad of or , and use && instead of and
        std::cout << "hi"; // and ! use for "not" e.g ! true = false
    }

    return 0;
}